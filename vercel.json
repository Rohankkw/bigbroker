{
  "framework": "vite",
  "installCommand": "npm ci --include=dev --prefer-online",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
